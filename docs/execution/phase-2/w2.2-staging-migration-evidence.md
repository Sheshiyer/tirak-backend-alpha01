# W2.2 — Staging Migration Application · Evidence (Stages 0–D)

Status: **DRAFT — PENDING GATE 2 HUMAN REVIEW. NOT MARKED COMPLETE.**
Executed: 2026-07-24 (UTC) · Branch: `codex/tirak-omise/w2.2/staging-migration-application`
Authority: T-032 signed GO (2026-07-24) + GATE 1 approval (2026-07-24, human release owner,
including the §6.4 variance adjudication: structural-only post-apply verification,
behavioral subset carried as proven by T-030/T-031).
Scope executed: Stages 0 → A → B → C → D. **Stage E publication NOT performed** (no push,
no PR, no issue comment) — awaiting GATE 2.

## Stage 0 — Preconditions: PASS

- `main` fast-forwarded to PR #22 merge `0ec636853d3402e5b44a90129f7ad22918a5b320`;
  branch created from it.
- Baseline regeneration: `node scripts/migrations/generate-canonical-baseline.mjs` →
  sha256 `b6532c80e5eeb6b481c26f5ad12f58043f8ad77587ea503527f6cb94e47cf33f` — **exact
  match** (abort trigger 3 not fired). Tree clean after regeneration.
- `.env.tirak-staging` present, mode `0600`. Credentials exported via sed only, never
  echoed.
- Note: untracked `.codegraph/` tooling directory present; tracked tree clean.
- Note: prior staging ledger `docs/execution/phase-2/t-025-staging-resource-ledger.json`
  (tracked) had mode `0644` after checkout; restored to `0600` to satisfy the discovery
  tool's owner-only invariant. No content change; git ignores the mode bit.

## Stage A — Fresh recovery point: PASS

- `npm run staging:preflight` → PASS (account-token verified, pinned account in scope,
  0 remote mutations).
- `npm run staging:discover` → pre-apply fingerprint
  `3b57299a2a7cadc048243a18aeae8cc6d568b548eacf3edcfaa5ddbc24eef7cc` — **exact match**
  (abort trigger 1 not fired).
- Pristine gate: `d1 info` → `num_tables: 0`, uuid
  `5132c8cc-8f23-4dd2-94d1-9d53edb92888` (abort trigger 2 not fired).
- Time Travel bookmark: `00000010-00000000-000050b2-3f8947f94d947d0f4981843872d6a7ab`
  (`evidence/w22/bookmark-pre.json`).
- Full export: `evidence/w22/tirak-staging-pre-20260724T153831Z.sql` (32 bytes —
  `PRAGMA defer_foreign_keys=TRUE;` only, consistent with a pristine DB). Signed export
  URL redacted from transcript per the T-026 rule.
- Checksums: `evidence/w22/checksums.sha256`.

## GATE 1 — Approved 2026-07-24 (human release owner)

Approval on record with the orchestrator, including the §6.4 variance adjudication
(structural-only post-apply verification on staging; behavioral subset carried as proven
by T-030/T-031).

## Stage B — Isolated apply roots + wrong-target verification: PASS

- `rehearsal/w22/migrations-baseline/` — `canonical-baseline.sql` only.
- `rehearsal/w22/migrations/` — `008_omise_promptpay_payments.sql`,
  `010_booking_chat_expansion.sql`, `011_payment_restitutions.sql` **only**; quarantined
  `004_mobile_app_features.sql` / `009_booking_scoped_chat.sql` verified absent.
- Dedicated configs `rehearsal/w22/wrangler.baseline.toml` and
  `rehearsal/w22/wrangler.lineage.toml` — repo `wrangler.toml` never used.
- Wrong-target double-verification (`evidence/w22/wrong-target-verification.txt`): both
  configs resolve to `database_name = tirak-staging`,
  `database_id = 5132c8cc-8f23-4dd2-94d1-9d53edb92888`,
  `account_id = 2c0c96c68f0ee73b6d980054557bca5b`; remote `d1 info` name→uuid resolution
  agrees (abort trigger 8 not fired).

## Stage C — Apply: PASS (first write to active staging in program history)

- Baseline root applied first: `canonical-baseline.sql` — **160 commands**, exit 0,
  transcript confirms remote target `tirak-staging (5132c8cc-8f23-4dd2-94d1-9d53edb92888)`.
- Lineage root applied second, filename order: `008_omise_promptpay_payments.sql`
  (7 commands) → `010_booking_chat_expansion.sql` (7 commands) →
  `011_payment_restitutions.sql` (4 commands), all ✅, exit 0.
- No errors, no retries (abort trigger 4 not fired). Transcripts sanitized under
  `evidence/w22/apply-*.sanitized.txt`.

## Stage D — Verification (structural-only per §6.4 variance): PASS

1. **Schema surface vs contract** — live `sqlite_schema` dump (160 objects: 41 tables
   incl. internal `_cf_KV` + `d1_migrations`, 119 indexes) replayed through
   `rehearsal/w22/live-surface-diff.mjs` (T-030 harness, re-pointed at the staging dump):
   **MATCH — 14/14 contract objects exact, zero missing, zero extra, zero diff problems**
   (`evidence/w22/surface-diff-result.json`). Companion suite
   `npx vitest run tests/migrations/`: **5 files / 29 tests passed**.
2. **`PRAGMA foreign_key_check`** — **zero rows** (`evidence/w22/fk-check-post.json`).
3. **Ledger** — `d1_migrations` dump, exactly 4 rows, canonical order, no quarantined
   names:

   | id | name | applied_at (UTC) |
   | --- | --- | --- |
   | 1 | canonical-baseline.sql | 2026-07-24 15:50:25 |
   | 2 | 008_omise_promptpay_payments.sql | 2026-07-24 15:52:44 |
   | 3 | 010_booking_chat_expansion.sql | 2026-07-24 15:52:44 |
   | 4 | 011_payment_restitutions.sql | 2026-07-24 15:52:45 |

   `node scripts/migrations/verify-lineage.mjs` → **exit 0** ("recognized, consistent
   lineage state"). Abort trigger 5 not fired.
4. **Row-count manifest** — 39 user tables (internal `_cf_%` / `sqlite_%` excluded;
   `_cf_KV` is SQLITE_AUTH-protected and was excluded per the discovery tool's
   internal-table rule): **ALL zero rows**; `d1_migrations` = 4 rows. Verdict CLEAN
   (`evidence/w22/row-counts-post.json`). No application data written.
5. **Post-apply fingerprint** — `npm run staging:discover` → new fingerprint
   **`8a27647d80ff4a6137469b16ee5786c3d950caf8c3e83b7f0e20698511797870`**
   (`evidence/w22/post-apply-fingerprint.txt`). The change is the expected,
   pre-registered one (staging now has schema). Discovery exit 2 /
   `PENDING_HUMAN_CONFIRMATION` is the tool's two-party hold — **flagged for human
   confirmation at GATE 2** as the successor baseline fingerprint. 0 remote mutations,
   0 production commands during discovery.

## Incidents / deviations (none affecting verdicts)

- Discovery initially refused: prior ledger file mode was `0644`; restored `0600` (Stage 0
  note). Re-run passed.
- `wrangler d1 execute --file` refused with `SQLITE_AUTH 7500` on the 40-subselect
  row-count query; root cause isolated to the internal `_cf_KV` table (querying it is
  authorizer-forbidden). Row counts completed via `--command` batches over the 39 user
  tables. Read-only throughout.

## State staging is left in (for GATE 2)

- `tirak-staging` (`5132c8cc-8f23-4dd2-94d1-9d53edb92888`): canonical baseline + 008 +
  010 + 011 applied; `num_tables: 40` (39 user + `d1_migrations`); all user tables empty;
  ledger exactly the 4 canonical rows; FK check clean; contract surface 14/14 exact.
- Recovery point before the write: bookmark
  `00000010-00000000-000050b2-3f8947f94d947d0f4981843872d6a7ab` + full export under
  `evidence/w22/`.
- No rollback invoked; BREAK-GLASS not triggered. No push, no PR, no issue comment.
- Awaiting GATE 2: human review of this evidence + confirmation of post-apply
  fingerprint `8a27647d…7870` as the successor baseline.
